# Data structure: container storage of diff values

# create a Tuple: can do list of numbers
coordinates = (4, 5)
coordinates_list = [(4, 5), (6,7), (80,90)]
#tuples = immutable =  once initialized, can't change ||  modify
# access by adding [] && passing in index
print(coordinates[1])
print(coordinates_list)
print(coordinates_list[0])
# Lists you can change and modify, tuples, no
List = [4,5]
List[0] = 7
print(List)

